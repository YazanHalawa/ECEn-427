/*
 * render.c
 *
 *  Created on: Sep 26, 2015
 *      Author: superman
 */
#include "render.h"
#include "globals.h"
#include "bitmaps.h"

void drawTank(int * framePointer0) {
	int row = 0;
	int col = 0;
	int current_position = getTankPositionGlobal();
	for (row = 384; row <= 400; row++) {
		for (col = current_position; col < current_position + 30; col++) {
			framePointer0[row*640 + col] = tank_15x8[row - 384];
		}
	}
}

void render() {
	//drawTank();
}
